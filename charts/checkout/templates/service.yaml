apiVersion: v1
kind: Service
metadata:
  name: checkout
  labels:
    {{- include "checkout.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "checkout.selectorLabels" . | nindent 4 }}
