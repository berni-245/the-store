# Default values for the catalog chart.
# CD overrides image.repository (GHCR) and image.tag (commit SHA) at deploy time.
# Local defaults point at the image built by local.sh and loaded into Kind.
image:
  repository: the-store-catalog
  tag: latest
  pullPolicy: IfNotPresent

replicaCount: 1

resources:
  limits:
    memory: 256Mi
  requests:
    cpu: 256m
    memory: 256Mi
